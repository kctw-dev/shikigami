# US-#319 Subagent 結果

**Story**：INFRA：出勤紀錄 per-session + 結算
**Sprint**：108
**完成時間**：2026-03-20T09:50:18+0800
**狀態**：DONE

---

## AC 完成狀態

| AC | 說明 | 狀態 |
|----|------|------|
| AC1 | 每個 session 寫入獨立 `YYYY-MM-DD-session-<session_id>.jsonl` | DONE |
| AC2 | `hooks/attendance-settle.sh` 合併同日檔案為 `YYYY-MM-DD.summary.jsonl` | DONE |
| AC3 | 跨機器不再 conflict（per-session 檔案天然隔離） | DONE |

---

## 修改清單

### 修改
- `hooks/session-start`：檔名改為 `YYYY-MM-DD-session-<SESSION_ID>.jsonl`；session_id=unknown 時使用 `unknown-$(date +%s)`；移除 flock（per-session 天然隔離）
- `hooks/session-end-release.sh`：同上，checkout 改用 per-session 檔名

### 新建
- `hooks/attendance-settle.sh`：結算腳本，掃描 per-session 檔案、依 timestamp 排序、合併為 `YYYY-MM-DD.summary.jsonl`，保留原始檔案（保守策略）
- `tests/test-attendance-settle.sh`：TDD 測試（14 個 TC，全部 PASS）

### 更新
- `docs/adr/ADR-024-attendance-hook.md`：新增 Amendment 段落，記錄從 per-day 到 per-session 的演化決策

---

## TDD 結果

```
=== 結果 ===
  PASS: 14
  FAIL: 0
[OK] test-attendance-settle.sh 全部通過
```

---

## 關鍵設計決策

- **flock 移除**：per-session 檔案每個 session 只有自己寫入，天然隔離，不需要 flock
- **unknown session 處理**：`unknown-$(date +%s)` 確保即使 `CLAUDE_SESSION_ID` 未設定，也不會覆寫其他 session
- **保守策略**：settle 後保留原始 per-session 檔案，不刪除
- **protect-main.sh**：`^docs/attendance/` 已涵蓋新命名格式，無需修改
