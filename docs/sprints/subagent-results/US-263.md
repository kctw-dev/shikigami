# US-263 Subagent Result

**Story**: US-263 — validate-version.sh 新增 README.md badge 版號檢查
**Date**: 2026-03-14
**Status**: PASS

## AC 驗收結果

| AC | 描述 | 結果 |
|----|------|------|
| AC1 | validate-version.sh 新增 README.md badge 版號檢查 | PASS |
| AC2 | badge 版號不一致時 FAIL | PASS |
| AC3 | 現有 AC1/AC1b/AC2 不受影響 | PASS |

## 實作摘要

### 修改檔案

- `scripts/validate-version.sh`：新增 `readonly README_FILE="README.md"` 常數及 `check_readme_badge_consistency` 函式
- `tests/test-validate-version.sh`：新增 TC-09/TC-10/TC-11 測試案例，並更新既有 TC-01~TC-08 加入 `write_readme_badge` 呼叫

### 實作細節

1. `readonly README_FILE="README.md"` 常數定義與 `PLUGIN_JSON`、`MARKETPLACE_JSON`、`GEMINI_EXTENSION_JSON` 同層
2. `check_readme_badge_consistency` 函式：
   - README 缺失 → `print_fail` → exit 1
   - 用 `grep -oP` 搭配 Perl regex 提取 badge 版號（格式：`![Version](https://img.shields.io/badge/version-v<VERSION>-blue?style=flat-square)`）
   - badge 版號與 `PLUGIN_VERSION` 比對，不一致 → `print_fail` → exit 1
3. 在 `main()` 中於 AC1b 後、AC2 前呼叫 `check_readme_badge_consistency`

### 測試結果

```
PASS=19  FAIL=3（TC-07 三項為既有 jq 缺失問題，與 US-263 無關）
```

新增測試案例：
- TC-09：README badge 版號與 plugin.json 一致 → PASS / exit 0
- TC-10：README badge 版號與 plugin.json 不一致 → FAIL / exit 1
- TC-11：README.md 缺失 → FAIL / exit 1

### 實際執行驗證

在 repo 根目錄執行 `bash scripts/validate-version.sh`，輸出：

```
--- AC-README：README.md badge 版號一致性
  plugin.json  version: 0.69.3
  README.md badge version: 0.69.3
[PASS] README.md badge 與 plugin.json 版號一致 (0.69.3)
```

## Commit

已包含於 commit `66149f6`（US-267 平行執行合批提交）
