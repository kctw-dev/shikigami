# US-#324 Subagent 結果

**Story**：FIX：ADR-027 CI 權限模型改為可選分層
**Sprint**：113
**完成日期**：2026-03-21
**狀態**：PASS

---

## 完成項目

### AC-1：workflow input permission_level
- `.github/workflows/sprint-dispatch.yml` 已含 `permission_level` input（choice: low/medium/high，default: medium）
- 狀態：已完成（Sprint 113 開始前已實作）

### AC-2：--allowedTools 動態組裝
- `Resolve permission level` step 已存在，用 case/switch 依 `$INPUT_PERMISSION_LEVEL` 組裝 `ALLOWED_TOOLS`
- low：`Read,Grep,Glob`；medium/high：`Bash,Read,Write,Edit,Grep,Glob,WebSearch,WebFetch`
- Sprint Execution 與 Sprint Review steps 均使用 `"$ALLOWED_TOOLS"`
- 狀態：已完成（Sprint 113 開始前已實作）

### AC-3：high 等級 Stakeholder Approval Gate
- `Stakeholder Approval Gate（high 等級）` step 已存在
- `SHIKIGAMI_APPROVERS` 空值 fail fast、`notify_issue` 未指定 fail fast
- polling 間隔 30 秒、timeout 30 分鐘（`$((30 * 60))`）
- approved → 繼續；rejected/timeout → exit 1
- 狀態：已完成（Sprint 113 開始前已實作）

### AC-4：ADR-027 Amendment 1 新增
- `docs/adr/ADR-027-ci-permissions.md` 新增 `## Amendment 1：權限分層（#324）` 段落
- 含分層表格（low/medium/high）、approval gate 說明、邊界案例、原決策相容性說明
- 狀態：**本次實作**

### AC-5：測試覆蓋
- `tests/test-runner-dispatch.sh` TC-11~TC-14 已覆蓋全部 AC
- 修正 TC-11~TC-13 pattern 中 `\|`（BRE）→ `|`（ERE）確保 `grep -qE` 正確執行
- 測試結果：56/56 PASS

---

## 修改檔案

| 檔案 | 修改類型 | 說明 |
|------|---------|------|
| `docs/adr/ADR-027-ci-permissions.md` | 新增段落 | Amendment 1 權限分層 |
| `tests/test-runner-dispatch.sh` | Bug Fix | 修正 TC-11~TC-13 ERE pattern（`\|` → `|`） |
| `docs/sprints/sprint_113.md` | 狀態更新 | #324 → 完成 |

---

## 測試結果

```
[US-#323][US-#324] test-runner-dispatch.sh — ALL PASS
Total: 56 | PASS: 56 | FAIL: 0
```

## 驗證結果

所有 `scripts/validate-*.sh` 全部通過。
