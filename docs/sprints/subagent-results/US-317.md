# US-317 Phase 1 & Phase 2 — Subagent 結果

## 狀態：PASS

## 完成時間
2026-03-20T02:06+08:00

## AC 驗收結果

| AC | 狀態 | 說明 |
|----|------|------|
| AC1 | PASS | sprint-planning SKILL.md 新增開始時間記錄 + 會議紀錄寫入步驟 |
| AC1.1 | PASS | frontmatter 格式（type, sprint, date, start_time, end_time, participants）定義於 §5.1 |
| AC1.2 | PASS | 結論格式（Sprint Goal, 選入 Stories, 決議事項）定義於 §5.1 |
| AC1.3 | PASS | sprint-review 及 retro 各自產生對應會議紀錄，定義於 §5.2 |

## 修改檔案

| 檔案 | 變更說明 |
|------|---------|
| `skills/sprint-planning/SKILL.md` | 新增開始時間記錄步驟、會議紀錄寫入步驟、§5.1 sprint-planning 格式定義、§5 產出文件表增列 |
| `skills/sprint-review/SKILL.md` | 新增 REVIEW_START_TIME / RETRO_START_TIME 記錄步驟、§2 步驟 7 sprint-review 紀錄、§3 步驟 8 retro 紀錄、§5.2 格式定義、§5 產出文件表增列兩列 |
| `tests/test-us317-meeting-records.sh` | 新增 10 個 TDD 測試案例（全部 PASS） |

## 測試結果

```
PASS: TC-01：sprint-planning SKILL.md 包含 docs/meetings 路徑
PASS: TC-02：sprint-planning SKILL.md 包含 sprint-planning event-type
PASS: TC-03：sprint-planning SKILL.md 包含 participants frontmatter 欄位
PASS: TC-04：sprint-planning SKILL.md §5 產出文件表包含 meetings
PASS: TC-05：sprint-planning SKILL.md 包含 start_time 欄位指引
PASS: TC-06：sprint-review SKILL.md 包含 docs/meetings 路徑
PASS: TC-07：sprint-review SKILL.md 包含 sprint-review event-type
PASS: TC-08：sprint-review SKILL.md 包含 retro event-type
PASS: TC-09：sprint-review SKILL.md §5 產出文件表包含 meetings
PASS: TC-10：sprint-review SKILL.md 包含 participants frontmatter 欄位

結果：PASS=10  FAIL=0
```

## Commits

- `c2470dd` — test: US-317 Phase 1 — 新增 Sprint 會議紀錄測試套件（TDD 紅燈）
- `ced9c59` — feat: US-317 Phase 1 — Sprint 儀式自動產生會議紀錄

## 架構說明

- 目錄：`docs/meetings/YYYY-MM-DD-<event-type>.md`（event-type: sprint-planning | sprint-review | retro）
- `docs/meetings/` 若不存在由 skill 執行 `mkdir -p docs/meetings` 建立
- 開始時間在流程第一步用 `date` 指令記錄到 shell 變數（START_TIME / REVIEW_START_TIME / RETRO_START_TIME）
- 結束時間在寫入會議紀錄時即時取得

## 驗證

全部 `scripts/validate-*.sh` 通過無誤。

---

# US-317 Phase 2 — 出勤時數

## 狀態：PASS

## 完成時間
2026-03-20T02:48+08:00

## AC 驗收結果

| AC | 狀態 | 說明 |
|----|------|------|
| AC1 | PASS | `hooks/session-start` 新增 checkin 邏輯，寫入 `docs/attendance/YYYY-MM-DD.jsonl` |
| AC2 | PASS | `hooks/session-end-release.sh` 新增 checkout 邏輯，寫入同一 JSONL |
| AC3 | PASS | JSONL 含 session_id, role, event, timestamp, repo 五個必要欄位 |
| AC4 | PASS | 兩端均使用 flock -x -w 5 保護，5 個 concurrent session 實測 PASS |
| AC5 | PASS | `hooks/protect-main.sh` 豁免清單新增 `^docs/attendance/` pattern |

## 修改檔案

| 檔案 | 變更說明 |
|------|---------|
| `hooks/session-start` | 新增出勤簽到邏輯（checkin，flock 保護，ATTENDANCE_DIR 可覆寫） |
| `hooks/session-end-release.sh` | 新增出勤簽退邏輯（checkout，flock 保護） |
| `hooks/protect-main.sh` | 豁免清單新增 `docs/attendance/**` |
| `docs/adr/ADR-024-attendance-hook.md` | 出勤機制決策文件（方案 A） |
| `tests/test-us317-attendance.sh` | 15 個 TDD 測試案例（全部 PASS） |

## 測試結果

```
PASS: TC-01：session-start 包含 docs/attendance 路徑
PASS: TC-02：session-start 包含 checkin event
PASS: TC-03：session-start 使用 flock 保護寫入
PASS: TC-04：session-end 包含 docs/attendance 路徑
PASS: TC-05：session-end 包含 checkout event
PASS: TC-06：session-end 使用 flock 保護寫入
PASS: TC-07：session-start JSONL 包含 session_id 欄位
PASS: TC-08：session-start JSONL 包含 role 欄位
PASS: TC-09：session-start JSONL 包含 timestamp 欄位
PASS: TC-10：session-start JSONL 包含 repo 欄位
PASS: TC-11：protect-main.sh 豁免清單包含 docs/attendance/
PASS: TC-12：ADR-024-attendance-hook.md 存在
PASS: TC-13：session-start 支援 ATTENDANCE_DIR 環境變數覆寫（測試友好）
PASS: TC-14：session-end JSONL 包含 session_id 欄位
PASS: TC-15：session-start 使用 date 指令產生 ISO 8601 timestamp

結果：PASS=15  FAIL=0
```

## Commits

- `3a392d7` — feat: #317 Phase 2 — 出勤時數 SessionStart/End hook 簽到/簽退

## 架構說明

- 目錄：`docs/attendance/YYYY-MM-DD.jsonl`
- SessionStart（checkin）寫入：role=scrum-master，event=checkin，timestamp=ISO 8601
- SessionEnd（checkout）寫入：role=scrum-master，event=checkout，timestamp=ISO 8601
- flock 使用 `/tmp/shikigami-attendance-${REPO_FP}.lock`，REPO_FP 以 sha256sum 取 repo root 前 8 碼
- `ATTENDANCE_DIR` 環境變數支援測試覆寫（測試隔離友好）
- ADR-024 記錄選定方案 A（方案 B MCP server / 方案 C git commit 均已評估並排除）

## 驗證

全部 `scripts/validate-*.sh` 通過無誤。

