# US-305 Subagent Result

**Story**：INFRA — 版號策略：當日 minor 降級 patch
**Sprint**：106
**完成時間**：2026-03-20 02:02:57
**狀態**：DONE

---

## 實作摘要

### 修改檔案

1. **`skills/deployment-readiness/SKILL.md`**
   - 在「Minor Bump 觸發條件」與「Major Bump 觸發條件」之間，新增「當日重複 Minor 降級為 Patch」子節
   - 包含完整的 bash 判斷邏輯、決策規則表格、範例說明、及 HARD-GATE 保護

2. **`tests/test-us305-minor-to-patch-downgrade.sh`**（新增）
   - 10 項測試，覆蓋 AC1 / AC2 / AC3
   - 全部通過（PASS=10, FAIL=0）

### Commit

```
1fe8f46 feat: #305 版號策略 — 當日 minor bump 自動降級為 patch
```

---

## AC 驗收結果

| AC | 說明 | 結果 |
|----|------|------|
| AC1 | 當日已有 minor bump 時，後續 bump 降級為 patch | PASS（TC-01, TC-07） |
| AC2 | 跨日第一次 bump 維持 minor（不受影響） | PASS（TC-02, TC-03） |
| AC3 | `git tag --sort=-creatordate` 查詢可正常運作；無 tag 不報錯，降級為 minor | PASS（TC-04, TC-05, TC-06） |

---

## 核心邏輯

```bash
LATEST_TAG=$(git tag --sort=-creatordate | head -1)
TAG_DATE=$(git log -1 --format='%cd' --date=short "$LATEST_TAG" 2>/dev/null)
TODAY=$(date '+%Y-%m-%d')
if [[ "$TAG_DATE" == "$TODAY" ]]; then
  # 當日已有 tag → patch bump
else
  # 跨日或無 tag → minor bump
fi
```

---

## 測試結果

```
========================================
 US-305 版號策略 — 當日 minor 降級 patch
========================================

PASS: TC-01 AC1：當日已有 tag → 降級為 patch bump
PASS: TC-02 AC2：跨日（昨天的 tag）→ 維持 minor bump
PASS: TC-03 AC2：更早的 tag 日期 → 維持 minor bump
PASS: TC-04 AC3：無任何 tag → 預設 minor bump（不報錯）
PASS: TC-05 AC3：git tag --sort=-creatordate 執行成功（exit 0）
PASS: TC-06 AC3：無 tag 的全新 repo → BUMP_TYPE=minor（不報錯）
PASS: TC-07 AC1：今日 tag 的 repo → BUMP_TYPE=patch（降級成功）
PASS: TC-08：SKILL.md 包含「當日重複 Minor 降級為 Patch」子節
PASS: TC-09：SKILL.md 包含 git tag --sort=-creatordate 查詢指令
PASS: TC-10：SKILL.md 降級子節含有 HARD-GATE 與無 tag 保護

========================================
 結果：PASS=10  FAIL=0
========================================
```
