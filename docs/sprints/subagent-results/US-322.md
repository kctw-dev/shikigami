# US-#322 Subagent 結果（Batch 1 + Batch 2）

**story_id**: US-#322 (Batch 1)
**completed_at**: 2026-03-21 14:23:03
**result**: PASS

## AC 結果摘要

| AC | 描述 | 結果 |
|----|------|------|
| AC-1 | Shoot_Log 改為 per-session + 結算 | PASS |
| AC-2 | sprint.live.log 改為 per-session + 結算 | PASS |
| AC-7 | PROJECT_BOARD.md push retry 機制 | PASS |

## 實作內容

### AC-1：Shoot_Log per-session

- 新增 `hooks/shoot-log-settle.sh`（結算腳本，合併同日 per-session .md 為 summary.md）
- 新增目錄 `docs/km/shoot-log/`（per-session 存放位置）
- 修改 `skills/shoot/SKILL.md` §9.1：寫入路徑改為 `docs/km/shoot-log/YYYY-MM-DD-session-<SESSION_ID>.md`
- 修改 `hooks/protect-main.sh`：豁免清單加入 `^docs/km/shoot-log/`

### AC-2：sprint.live.log per-session

- 新增 `hooks/live-log-settle.sh`（結算腳本，合併同日 per-session .log 為 summary.log）
- 新增目錄 `docs/sprints/live-log/`（per-session 存放位置）
- 修改 `skills/sprint-execution/story-lifecycle-prompt.md`：
  - 步驟 8 加入 SESSION_ID 初始化邏輯，路徑改為 `docs/sprints/live-log/YYYY-MM-DD-session-<SESSION_ID>.log`
  - 所有 `>> docs/sprints/sprint.live.log` 改為 `>> "${LIVE_LOG_FILE:-...}"`
- 修改 `skills/sprint-execution/SKILL.md` Live Log 說明章節：更新路徑描述
- 修改 `hooks/protect-main.sh`：豁免清單加入 `^docs/sprints/live-log/`

### AC-7：push retry 機制

- 修改 `skills/sprint-execution/SKILL.md` §3 步驟 7 狀態文件更新段落：加入 push retry 說明（`git pull --rebase` + 最多 3 次重試）

## 測試

| 測試檔案 | TC 數量 | 結果 |
|---------|---------|------|
| tests/test-us322-shoot-log-per-session.sh | 10 | 10 PASS |
| tests/test-us322-live-log-per-session.sh | 11 | 11 PASS |
| tests/test-us322-push-retry.sh | 3 | 3 PASS |
| 驗證腳本 (validate-*.sh) | 全部 | 全部 PASS |

**總計：24 PASS / 0 FAIL**

## 跨機器考量

- per-session 檔名含 `SESSION_ID`，不同機器寫入不同檔案，天然隔離
- `SESSION_ID` 無值時 fallback 為 `unknown-$(date +%s)`，防止碰撞
- 結算腳本獨立執行，不需 flock
- protect-main.sh 豁免新路徑，允許直推 main

---

# US-#322 Batch 2 — Subagent 結果

**story_id**: US-#322 (Batch 2)
**completed_at**: 2026-03-21 14:32:04
**result**: PASS

## AC 結果摘要

| AC | 描述 | 結果 |
|----|------|------|
| AC-3 | Metrics_Log.md 改為 per-session + 結算 | PASS |
| AC-4 | Retrospective_Log.md 改為 per-session + 結算 | PASS |
| AC-5 | quality-gate-decisions.md 改為 per-session + 結算 | PASS |
| AC-6 | Tech_Debt_Registry.md 改為 per-session + 結算 | PASS |
| AC-8 | sprint_N.md push retry 機制確認 | PASS |
| AC-9 | 所有修改的 Skill 文件更新指引 | PASS |
| AC-10 | 測試覆蓋（tests/test-us322-batch2.sh）| PASS |

## 實作內容

### AC-3：Metrics_Log per-session

- 新增 `hooks/metrics-settle.sh`（結算腳本，合併同日 per-session .md 為 summary.md）
- 新增目錄 `docs/km/metrics-log/`（per-session 存放位置，含 .gitkeep）
- `skills/sprint-review/SKILL.md`：§3 步驟 1/6、§5 產出文件表、§6 歸檔觸發檢查均已引用 `docs/km/metrics-log/YYYY-MM-DD-session-<SESSION_ID>.md`
- `hooks/protect-main.sh`：豁免清單加入 `^docs/km/metrics-log/`

### AC-4：Retrospective_Log per-session

- 新增 `hooks/retro-settle.sh`（結算腳本，合併同日 per-session .md 為 summary.md）
- 新增目錄 `docs/km/retro-log/`（per-session 存放位置，含 .gitkeep）
- `skills/sprint-review/SKILL.md`：§3 步驟 1/6、§5 產出文件表、§6 歸檔觸發檢查均已引用 `docs/km/retro-log/YYYY-MM-DD-session-<SESSION_ID>.md`
- `hooks/protect-main.sh`：豁免清單加入 `^docs/km/retro-log/`

### AC-5：quality-gate-decisions per-session

- 新增 `hooks/quality-decisions-settle.sh`（結算腳本，合併同日 per-session .md 為 summary.md）
- 新增目錄 `docs/km/quality-decisions/`（per-session 存放位置，含 .gitkeep）
- `skills/quality-gate/SKILL.md` §7.1/§7.2：引用 `docs/km/quality-decisions/YYYY-MM-DD-session-<SESSION_ID>.md`（B/C 選項強制記錄）
- `hooks/protect-main.sh`：豁免清單加入 `^docs/km/quality-decisions/`

### AC-6：Tech_Debt_Registry per-session

- 新增 `hooks/tech-debt-settle.sh`（結算腳本，合併同日 per-session 表格列為 summary.md，含表頭重建）
- 新增目錄 `docs/km/tech-debt/`（per-session 存放位置，含 .gitkeep）
- `skills/sprint-execution/story-lifecycle-prompt.md` §11：引用 `docs/km/tech-debt/YYYY-MM-DD-session-<SESSION_ID>.md`
- `hooks/protect-main.sh`：豁免清單加入 `^docs/km/tech-debt/`

### AC-8：sprint_N.md push retry 確認

- `skills/sprint-execution/SKILL.md` 已包含 Push Retry 機制（TC-13 PASS），涵蓋 sprint_N.md 更新情境

### AC-9：所有 Skill 文件路徑更新確認

- sprint-review/SKILL.md：metrics-log/ + retro-log/ 路徑全面更新
- quality-gate/SKILL.md：quality-decisions/ 路徑更新（§7.1、§7.2）
- sprint-execution/story-lifecycle-prompt.md：tech-debt/ 路徑更新（§11）
- protect-main.sh：4 個新豁免路徑全部加入

### AC-10：測試

- `tests/test-us322-batch2.sh`：21 個 TC，0 FAIL
- 修復測試腳本的 trap/unset set -u 相容性問題（統一 cleanup trap + `:-` 預設值）

## 測試

| 測試檔案 | TC 數量 | 結果 |
|---------|---------|------|
| tests/test-us322-batch2.sh | 21 | 21 PASS |

**總計：21 PASS / 0 FAIL**

## 跨機器考量

- 4 個 KM 類 per-session 目錄均採用 `YYYY-MM-DD-session-<SESSION_ID>.md` 命名，不同機器天然隔離
- SESSION_ID 取自 `${CLAUDE_SESSION_ID:-unknown}`
- 結算腳本可獨立執行，不需 flock
- protect-main.sh 豁免清單完整涵蓋所有新路徑，允許直推 main
