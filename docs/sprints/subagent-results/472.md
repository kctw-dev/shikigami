# Story #472 Subagent Result

**Story**: [SRE] CI failure: New Issue Intake — unzip 缺失問題復發（2026-03-23）
**Sprint**: 125
**Type**: INFRA
**Size**: S (1pt)
**Date**: 2026-03-24
**Status**: PASS

## 結果摘要

**PASS**
**MERGED_COMMIT**: a2079500b05fdb8ceb41c90b1a68c0e90a0a0f41
**PR**: https://github.com/kctw-dev/shikigami/pull/477

## 實作說明

### 根因分析（#442 修復失效原因）

| 修復版本 | Commit | 方案 | 結果 |
|---------|--------|------|------|
| #442 初次修復 | 01dbf56 | `sudo apt-get install -y unzip` | 正確 |
| #464 引入回歸 | a6e8a4a | `sudo -n apt-get ... 2>/dev/null || echo "proceeding anyway"` | 靜默失敗 |
| #472 本次修復 | a207950 | 回歸 `sudo apt-get install -y unzip`（無 -n） | 正確 |

**失效鏈**：`sudo -n` 在 self-hosted runner 無 NOPASSWD 設定時立即退出 → `2>/dev/null` 靜默吞錯誤 → `|| echo` fallback 使整行退出碼為 0 → unzip 未安裝 → setup-bun 下載 Bun 時 unzip 缺失失敗（85% failure rate）。

### 修改檔案

- `.github/workflows/new-issue-intake.yml`：回歸 `sudo apt-get install -y unzip`，移除 sudo -n 靜默失敗模式
- `tests/test-472-unzip-regression-fix.sh`：新增，7 assertions
- `tests/test-464-unzip-sudo-regression.sh`：更新，移除已廢棄的 sudo -n 斷言

## AC 自檢

| AC | 狀態 | 說明 |
|----|------|------|
| AC1 | PASS | `sudo apt-get install -y unzip` 步驟位於 setup-bun 之前（line 24 vs 34） |
| AC2 | PASS | `command -v unzip` 守衛，有快取時靜默跳過 |
| AC3 | PASS | 冪等性守衛覆蓋快取/無快取兩種路徑 |
| AC4 | 待 CI | 合入後觸發 5 次確認（workflow 已部署） |
| AC5 | PASS | 回歸根因記錄於 PR #477 description |

## 測試結果

- test-472-unzip-regression-fix.sh: PASS 7/7
- test-464-unzip-sudo-regression.sh: PASS 4/4
- test-442-ci-unzip-fix.sh: PASS 6/6
- validate-ci-versions.sh: PASS
- 全部 validate-*.sh: PASS
